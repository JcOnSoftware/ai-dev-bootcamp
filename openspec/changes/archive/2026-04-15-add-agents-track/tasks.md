# Tasks: add-agents-track

Change: `add-agents-track`
Phase: tasks
Total: 64 tasks across 7 batches (Batch 0 + Batches 1–5 + Batch 6)
Strict TDD: ENABLED — tests-first always, solution only written after FAIL confirmed.

---

## Batch 0 — Shared fixture (4 tasks)

- [ ] B0-T01: Create dir `code/packages/exercises/05-agents/fixtures/`
- [ ] B0-T02: Write `code/packages/exercises/05-agents/fixtures/research-tools.ts` — export `SEARCH_DOCS_TOOL` (name `search_docs`, description mentions returns chunk ids + topic metadata NOT full content, input schema: `query` required string, `top_k` optional number); export `READ_CHUNK_TOOL` (name `read_chunk`, description mentions returns full text by id, input schema: `id` required string); export `AGENT_TOOLS = [SEARCH_DOCS_TOOL, READ_CHUNK_TOOL] as const`; export `executeSearchDocs(input: { query: string; top_k?: number }): string` — case-insensitive substring match on `chunk.text` AND `chunk.metadata.topic`, default top_k=3, returns `JSON.stringify([{ id, topic, source }, ...])` (NO `.text` in results); export `executeReadChunk(input: { id: string }): string` — returns `JSON.stringify({ id, content: chunk.text, metadata })` or `{ error: "Chunk not found: <id>" }` if not found; export `executeTool(name: string, input: unknown): string` — routes to `executeSearchDocs`/`executeReadChunk`, returns `{ error: "Unknown tool: <name>" }` for unknown names. Import `DOCS_CHUNKS` and `Chunk` type from `../../04-rag/fixtures/docs-chunks.ts`. Use `chunk.text` (NOT `.content`) throughout.
- [ ] B0-T03: `bunx tsc --noEmit` from `code/` → zero errors
- [ ] B0-T04: Commit: `feat(exercises/05-agents): add shared research-tools fixture`

---

## Batch 1 — 01-agent-loop (11 tasks)

- [ ] B1-T01: Create dirs `code/packages/exercises/05-agents/01-agent-loop/`, `es/`, `en/`
- [ ] B1-T02: Write `tests.test.ts` — integration describe with `beforeAll` guard `ANTHROPIC_API_KEY`; uses `resolveExerciseFile(import.meta.url)` + `AIDEV_TARGET` + harness; test 1 "runs agent loop and terminates with end_turn": `result.calls.length >= 1 && <= 10`; `result.calls[0].request.tools.length === 2`; `result.calls[0].request.tools` has entries with `name === "search_docs"` and `name === "read_chunk"` (via `.find()`); `result.calls[0].request.model` matches `/haiku/i`; `result.calls[result.calls.length-1].response.stop_reason === "end_turn"`; `result.userReturn.iterations >= 1 && <= 10`; `result.userReturn.finalMessage.content` has at least one block with `b.type === "text"`. Add `{ timeout: 30000 }` to the test.
- [ ] B1-T03: Write `starter.ts` — `// Docs:` header with `https://docs.claude.com/en/docs/agents-and-tools/tool-use/overview` and `https://docs.claude.com/en/docs/agents-and-tools/tool-use/implement-tool-use`; import `AGENT_TOOLS, executeTool` from `../fixtures/research-tools.ts`; import `Anthropic` from `@anthropic-ai/sdk`; export `async function runAgentLoop(userQuery: string, maxIterations = 10): Promise<{ finalMessage: Anthropic.Message; iterations: number }>` that throws `new Error("TODO: implementa runAgentLoop() — lee el ejercicio en es/exercise.md")`; export `default async function run()` that throws `new Error("TODO: implementa run()")`.
- [ ] B1-T04: `AIDEV_TARGET=starter bun test packages/exercises/05-agents/01-agent-loop` from `code/` → MUST fail (confirm TODO errors)
- [ ] B1-T05: Write `solution.ts` — model `claude-haiku-4-5-20251001`; system prompt instructs Claude to use provided tools to answer questions about Anthropic docs; `runAgentLoop` builds initial `messages: Anthropic.MessageParam[]` with user query, includes `AGENT_TOOLS` in every `messages.create` call; while loop: send `messages.create`, increment `iterations`, if `stop_reason === "end_turn"` break + return `{ finalMessage, iterations }`; if any block `type === "tool_use"` → `executeTool(block.name, block.input)`, append assistant message + `tool_result` user message, continue; if `iterations >= maxIterations` break + return; `run()` calls `runAgentLoop("What's the TTL for prompt caching?", 10)`.
- [ ] B1-T06: `AIDEV_TARGET=solution bun test packages/exercises/05-agents/01-agent-loop` from `code/` → MUST pass against real Anthropic API
- [ ] B1-T07: Write `es/exercise.md` — 6 sections per contract: `# Ejercicio 01 — Agent Loop`; `## Concepto` (define agent as think→act→observe cycle drawn in text, explain why DIY loop beats SDK Tool Runner for learning, explain `stop_reason: "tool_use"` vs `stop_reason: "end_turn"`); `## Docs & referencias` (canonical docs.claude.com URLs numbered); `## Tu tarea` (step-by-step impl guide for `runAgentLoop` and `run()`); `## Cómo verificar` (`aidev verify 01-agent-loop` + bullet list of what tests check); `## Concepto extra (opcional)` (mention `client.beta.messages.toolRunner()` link `https://docs.claude.com/en/docs/agents-and-tools/tool-use/tool-runner`, note: abstracts the pedagogy — learn loop first, use Tool Runner in production)
- [ ] B1-T08: Write `en/exercise.md` — 6 equivalent sections in English
- [ ] B1-T09: Write `meta.json` — `{"id":"01-agent-loop","track":"05-agents","title":"Agent Loop","version":"1.0.0","valid_until":"2026-10-15","concepts":["agent-loop","tool-use","stop-reason","think-act-observe","diy-loop"],"estimated_minutes":25,"requires":["01-first-call"],"locales":["es","en"],"model_cost_hint":"~$0.002 (Haiku 4.5, 1–3 iterations)"}`
- [ ] B1-T10: `bun packages/cli/src/index.ts list` from `code/` → confirm `01-agent-loop` appears under `05-agents`
- [ ] B1-T11: Commit: `feat(exercises/05-agents/01-agent-loop): add agent loop exercise`

---

## Batch 2 — 02-stop-conditions (11 tasks)

- [ ] B2-T01: Create dirs `code/packages/exercises/05-agents/02-stop-conditions/`, `es/`, `en/`
- [ ] B2-T02: Write `tests.test.ts` — pure unit `describe("unit — evaluateStop", ...)` (no API): 4 tests: mock message `stop_reason: "end_turn"` no tool_use → `"end_turn"`; mock message `stop_reason: "tool_use"` iteration >= maxIterations → `"max_iterations"`; goalPredicate returns true → `"goal_reached"`; `stop_reason: "tool_use"` + iteration < max + no goal → `"continue"` (construct minimal `Anthropic.Message`-shaped objects, no SDK import required for mock shape); integration `describe` with `beforeAll` guard `ANTHROPIC_API_KEY`: test 1 "stops with stoppedReason in valid set": `result.userReturn` has fields `stoppedReason`, `iterations`, `finalMessage`; `result.userReturn.stoppedReason` is one of `"end_turn" | "max_iterations" | "goal_reached"`; `result.userReturn.iterations >= 1 && <= 10`; test 2 "forces max_iterations by passing maxIterations: 1": calls `runWithStopConditions` directly with `maxIterations: 1`, asserts `stoppedReason === "max_iterations"`. Timeout 30000 on integration tests.
- [ ] B2-T03: Write `starter.ts` — `// Docs:` header (same two URLs as 01); imports `AGENT_TOOLS, executeTool` from `../fixtures/research-tools.ts`; export `function evaluateStop(message: Anthropic.Message, iteration: number, maxIterations: number, goalPredicate?: (msg: Anthropic.Message) => boolean): "end_turn" | "max_iterations" | "goal_reached" | "continue"` → throws TODO; export `async function runWithStopConditions(userQuery: string, options: { maxIterations: number; goalPredicate?: (msg: Anthropic.Message) => boolean }): Promise<{ stoppedReason: "end_turn" | "max_iterations" | "goal_reached"; iterations: number; finalMessage: Anthropic.Message }>` → throws TODO; export `default async function run()` → throws TODO.
- [ ] B2-T04: `AIDEV_TARGET=starter bun test packages/exercises/05-agents/02-stop-conditions` from `code/` → MUST fail
- [ ] B2-T05: Write `solution.ts` — model `claude-haiku-4-5-20251001`; implement `evaluateStop` pure function: checks goalPredicate first (returns `"goal_reached"`), then `end_turn` (returns `"end_turn"`), then `iteration >= maxIterations` (returns `"max_iterations"`), else `"continue"`; `runWithStopConditions` DIY while loop using `evaluateStop` to determine action; `run()` passes goalPredicate that checks whether response text contains "5 minutes" or "5-minute" (case-insensitive), query "What is the TTL for prompt caching?", `maxIterations: 10`.
- [ ] B2-T06: `AIDEV_TARGET=solution bun test packages/exercises/05-agents/02-stop-conditions` from `code/` → MUST pass
- [ ] B2-T07: Write `es/exercise.md` — 6 sections: `# Ejercicio 02 — Stop Conditions`; `## Concepto` (side-by-side table of 3 conditions with When/Use for columns, explain WHY all 3 needed: model alone can loop forever, hard cap without goal wastes tokens, goal without cap unsafe); `## Docs & referencias`; `## Tu tarea` (guide for `evaluateStop` + `runWithStopConditions`); `## Cómo verificar` (`aidev verify 02-stop-conditions`); `## Concepto extra (opcional)` (production goal predicates often parse structured JSON from model response, e.g. `{ "done": true, "answer": "..." }`)
- [ ] B2-T08: Write `en/exercise.md` — 6 equivalent sections in English
- [ ] B2-T09: Write `meta.json` — `{"id":"02-stop-conditions","track":"05-agents","title":"Stop Conditions","version":"1.0.0","valid_until":"2026-10-15","concepts":["stop-conditions","goal-predicate","max-iterations","termination-logic"],"estimated_minutes":30,"requires":["01-agent-loop"],"locales":["es","en"],"model_cost_hint":"~$0.005 (Haiku 4.5, multiple stop condition tests)"}`
- [ ] B2-T10: `bun packages/cli/src/index.ts list` from `code/` → confirm `02-stop-conditions` appears under `05-agents`
- [ ] B2-T11: Commit: `feat(exercises/05-agents/02-stop-conditions): add stop conditions exercise`

---

## Batch 3 — 03-state-management (11 tasks)

- [ ] B3-T01: Create dirs `code/packages/exercises/05-agents/03-state-management/`, `es/`, `en/`
- [ ] B3-T02: Write `tests.test.ts` — integration `describe` with `beforeAll` guard `ANTHROPIC_API_KEY`; uses harness + `resolveExerciseFile` + `AIDEV_TARGET`; test 1 "maintains conversation state across turns": `result.userReturn.state.messages.length >= 4` (2 user + ≥2 assistant; tool blocks may inflate); last assistant reply content has at least one `type === "text"` block with `.text.length > 20`; `result.userReturn.state.metadata.totalIterations >= 2 && <= 15`; the final user message in `state.messages` has `role === "user"` and content equal to `"And the extended TTL option?"` (handle both string and array `ContentBlockParam` forms). Timeout 30000.
- [ ] B3-T03: Write `starter.ts` — `// Docs:` header; imports `AGENT_TOOLS, executeTool`; export `interface ConversationState { messages: Anthropic.MessageParam[]; metadata: { totalIterations: number; startedAt: string } }`; export `function startConversation(): ConversationState` → throws TODO; export `async function continueConversation(state: ConversationState, userMessage: string): Promise<{ state: ConversationState; reply: Anthropic.Message }>` → throws TODO; export `default async function run()` → throws TODO.
- [ ] B3-T04: `AIDEV_TARGET=starter bun test packages/exercises/05-agents/03-state-management` from `code/` → MUST fail
- [ ] B3-T05: Write `solution.ts` — model `claude-haiku-4-5-20251001`; `startConversation()` returns `{ messages: [], metadata: { totalIterations: 0, startedAt: new Date().toISOString() } }`; `continueConversation`: appends userMessage to `state.messages`, runs DIY agent loop until `end_turn` or `maxIterations: 10`, accumulates ALL messages (including tool_use/tool_result turns) into `state.messages`, increments `state.metadata.totalIterations` by loop count, returns `{ state, reply: finalMessage }`; `run()`: 2-turn conversation — turn 1 "What's the TTL default for prompt caching?", turn 2 "And the extended TTL option?" (uses prior history — only resolvable with full messages array), returns `{ state, reply }` from turn 2. `ConversationState` defined LOCAL to this file (ADR-8, not lifted to fixture).
- [ ] B3-T06: `AIDEV_TARGET=solution bun test packages/exercises/05-agents/03-state-management` from `code/` → MUST pass
- [ ] B3-T07: Write `es/exercise.md` — 6 sections: `# Ejercicio 03 — State Management`; `## Concepto` (Claude's API is stateless — model has NO memory between calls, must send full `messages[]` every request; `ConversationState` is simplest externalization; more advanced: SQLite, Redis, vector stores; explain why turn 2 "the extended" only works with history — without messages[], it's an unresolvable pronoun reference); `## Docs & referencias`; `## Tu tarea`; `## Cómo verificar`; `## Concepto extra (opcional)` (external state stores: SQLite, Redis, vector DBs for long-term memory; context window management: summarization, sliding window for very long conversations)
- [ ] B3-T08: Write `en/exercise.md` — 6 equivalent sections in English
- [ ] B3-T09: Write `meta.json` — `{"id":"03-state-management","track":"05-agents","title":"State Management","version":"1.0.0","valid_until":"2026-10-15","concepts":["conversation-state","message-history","multi-turn","stateless-api"],"estimated_minutes":30,"requires":["02-stop-conditions"],"locales":["es","en"],"model_cost_hint":"~$0.005 (Haiku 4.5, 2-turn conversation)"}`
- [ ] B3-T10: `bun packages/cli/src/index.ts list` from `code/` → confirm `03-state-management` appears under `05-agents`
- [ ] B3-T11: Commit: `feat(exercises/05-agents/03-state-management): add state management exercise`

---

## Batch 4 — 04-multi-step-plan (11 tasks)

- [ ] B4-T01: Create dirs `code/packages/exercises/05-agents/04-multi-step-plan/`, `es/`, `en/`
- [ ] B4-T02: Write `tests.test.ts` — integration `describe` with `beforeAll` guard `ANTHROPIC_API_KEY`; uses harness + `resolveExerciseFile` + `AIDEV_TARGET`; test "decomposes query into multiple distinct tool calls": `result.userReturn.iterations >= 2 && <= 10`; `result.userReturn.toolCallsMade.length >= 2`; at least 2 entries with `name === "search_docs"` (via `.filter()`); those 2+ `search_docs` entries have DISTINCT `input.query` values (normalize: `.toLowerCase().trim()`, assert `queryA !== queryB`); `result.userReturn.finalAnswer.length > 50`; soft semantic: `finalAnswer` contains both `"1.25"` and `"2"` as substrings (the cache write/read multipliers), OR matches `/(1\.25|1\.25x|25%).*?(2x|double|200%|two times)|(2x|double|200%|two times).*?(1\.25|1\.25x|25%)/i`. Timeout 30000.
- [ ] B4-T03: Write `starter.ts` — `// Docs:` header; imports `AGENT_TOOLS, executeTool`; export `async function runMultiStepPlan(userQuery: string, maxIterations = 10): Promise<{ finalAnswer: string; toolCallsMade: { name: string; input: unknown }[]; iterations: number }>` → throws TODO; export `default async function run()` → throws TODO.
- [ ] B4-T04: `AIDEV_TARGET=starter bun test packages/exercises/05-agents/04-multi-step-plan` from `code/` → MUST fail
- [ ] B4-T05: Write `solution.ts` — model `claude-haiku-4-5-20251001`; system prompt MUST contain explicit planning instruction such as "Before answering, plan your steps. Search for each piece of information separately, then synthesize a final answer." (this is a spec-level requirement — demonstrates prompt engineering for task decomposition); DIY loop accumulating `toolCallsMade` by collecting every `tool_use` content block's `{ name, input }` pair across all iterations; `run()` query: "Compare the cost multipliers for 5-minute vs 1-hour prompt caching. Give me both numbers side by side."; `finalAnswer` extracted from last text block of final `end_turn` message.
- [ ] B4-T06: `AIDEV_TARGET=solution bun test packages/exercises/05-agents/04-multi-step-plan` from `code/` → MUST pass
- [ ] B4-T07: Write `es/exercise.md` — 6 sections: `# Ejercicio 04 — Multi-Step Plan`; `## Concepto` (planning prompts change how model decomposes problem — without "plan first", model may issue one broad search and stop; explain why ≥2 distinct `search_docs` queries are the quality signal: one for "5-minute TTL" and one for "1-hour TTL" shows model internalized sub-problems; contrast with naive one-shot retrieval — show token/quality tradeoff); `## Docs & referencias`; `## Tu tarea`; `## Cómo verificar`; `## Concepto extra (opcional)` (Chain-of-Thought "think step by step" vs explicit planning prompts; extended thinking mode `"thinking": { "type": "enabled" }` internalizes planning — Sonnet/Opus only)
- [ ] B4-T08: Write `en/exercise.md` — 6 equivalent sections in English
- [ ] B4-T09: Write `meta.json` — `{"id":"04-multi-step-plan","track":"05-agents","title":"Multi-Step Plan","version":"1.0.0","valid_until":"2026-10-15","concepts":["task-decomposition","planning-prompt","multi-step","prompt-engineering"],"estimated_minutes":35,"requires":["03-state-management"],"locales":["es","en"],"model_cost_hint":"~$0.010 (Haiku 4.5, maxIterations:10 cap)"}`
- [ ] B4-T10: `bun packages/cli/src/index.ts list` from `code/` → confirm `04-multi-step-plan` appears under `05-agents`
- [ ] B4-T11: Commit: `feat(exercises/05-agents/04-multi-step-plan): add multi-step plan exercise`

---

## Batch 5 — 05-self-correction (11 tasks)

- [ ] B5-T01: Create dirs `code/packages/exercises/05-agents/05-self-correction/`, `es/`, `en/`
- [ ] B5-T02: Write `tests.test.ts` — integration `describe` with `beforeAll` guard `ANTHROPIC_API_KEY`; uses harness + `resolveExerciseFile` + `AIDEV_TARGET`; test "recovers from tool error": `result.userReturn.retries >= 1`; `result.userReturn.iterations >= 2 && <= 10`; across all `result.calls`, collect all `tool_use` content blocks of type `"tool_use"` with name `"read_chunk"` — assert at least 2 with DIFFERENT `input.id` values; specifically: one `id` matching `"nonexistent-id"` and at least one valid id present in `DOCS_CHUNKS` (import `DOCS_CHUNKS` in test or check against known ids like `"caching-01"`); `result.userReturn.finalMessage.content` has at least one `type === "text"` block with `.text.length > 30`. Timeout 30000.
- [ ] B5-T03: Write `starter.ts` — `// Docs:` header; imports `AGENT_TOOLS, executeTool`; export `async function runWithSelfCorrection(userQuery: string, maxIterations = 10): Promise<{ finalMessage: Anthropic.Message; retries: number; iterations: number }>` → throws TODO; export `default async function run()` → throws TODO.
- [ ] B5-T04: `AIDEV_TARGET=starter bun test packages/exercises/05-agents/05-self-correction` from `code/` → MUST fail
- [ ] B5-T05: Write `solution.ts` — model `claude-haiku-4-5-20251001`; system prompt MUST include: "If any tool returns { error }, try a DIFFERENT approach rather than retrying the same call." (spec-level requirement); `runWithSelfCorrection` DIY loop tracking `retries`: increment each time a `tool_result` string from `executeTool` contains `"error"` in its JSON; `run()` query: "Read chunk 'nonexistent-id' and summarize it. If that chunk doesn't exist, try 'caching-01' instead." (expected: agent calls `read_chunk("nonexistent-id")` → error → recovers with `read_chunk("caching-01")` → end_turn).
- [ ] B5-T06: `AIDEV_TARGET=solution bun test packages/exercises/05-agents/05-self-correction` from `code/` → MUST pass
- [ ] B5-T07: Write `es/exercise.md` — 6 sections: `# Ejercicio 05 — Self-Correction`; `## Concepto` (define error observation: agent sees `{ error }` in `tool_result` block — part of conversation history, model CAN reason over it if system prompt instructs recovery; recovery pattern: system prompt = contract, "if tool returns error, try different approach" is a behavioral constraint; explain `retries` counter: counts error-containing tool results, NOT iterations — loop with 3 iterations but 1 error has `retries: 1`); `## Docs & referencias`; `## Tu tarea`; `## Cómo verificar`; `## Concepto extra (opcional)` (`disable_parallel_tool_use: true` in `tool_choice` — forces sequential tool execution, making error propagation predictable and avoiding partial-failure states)
- [ ] B5-T08: Write `en/exercise.md` — 6 equivalent sections in English
- [ ] B5-T09: Write `meta.json` — `{"id":"05-self-correction","track":"05-agents","title":"Self-Correction","version":"1.0.0","valid_until":"2026-10-15","concepts":["self-correction","error-recovery","tool-errors","retry-logic"],"estimated_minutes":35,"requires":["04-multi-step-plan"],"locales":["es","en"],"model_cost_hint":"~$0.005 (Haiku 4.5, deliberate error + recovery)"}`
- [ ] B5-T10: `bun packages/cli/src/index.ts list` from `code/` → confirm `05-self-correction` appears under `05-agents`
- [ ] B5-T11: Commit: `feat(exercises/05-agents/05-self-correction): add self-correction exercise`

---

## Batch 6 — Final validation (5 tasks)

- [ ] B6-T01: `bunx tsc --noEmit` from `code/` → zero TypeScript errors
- [ ] B6-T02: `bun test packages/cli packages/runner` from `code/` → non-integration suite GREEN (expect ~104 pass, 0 fail)
- [ ] B6-T03: `AIDEV_TARGET=solution bun test packages/exercises/05-agents/` from `code/` → all 5 integration suites GREEN (requires `ANTHROPIC_API_KEY`; default Bun concurrency fine — no Voyage rate limit)
- [ ] B6-T04: `bun packages/cli/src/index.ts list` from `code/` → visually confirm 5 exercises under `05-agents` in correct order (`01-agent-loop` … `05-self-correction`); check both `--locale es` and `--locale en`
- [ ] B6-T05: `git log --oneline -10` → confirm 6 commits (fixture + 5 exercises) with `feat(exercises/05-agents/...)` conventional format; no `Co-Authored-By` lines
