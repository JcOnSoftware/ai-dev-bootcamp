# Skill Registry — ai-dev-bootcamp

Auto-generated by `sdd-init`. Keeps pre-resolved compact rules that sub-agents receive without reading full SKILL.md files.

## Project conventions

No project-level agent config (`AGENTS.md`, `CLAUDE.md`, `.cursorrules`) found. Conventions come from:
- User global: `~/.claude/CLAUDE.md`
- Project instructions: `~/work/CLAUDE.md` (workspace), `~/CLAUDE.md` (home)
- Project plan & contract: `docs/PLAN.md`, `docs/EXERCISE-CONTRACT.md`

## User skills (available for delegation)

| Skill | Purpose | Trigger |
|-------|---------|---------|
| `go-testing` | Go testing patterns, Bubbletea TUI | Go tests, teatest. **NOT APPLICABLE** to this TS/Bun project. |
| `skill-creator` | Create new AI agent skills | Adding new skills |
| `branch-pr` | PR creation workflow | Opening a PR |
| `issue-creation` | Issue creation workflow | Creating a GitHub issue |
| `judgment-day` | Dual adversarial review | User says "judgment day" / "doble review" |
| `api-documentation-generator` | API doc generation | API documentation tasks |

## Compact rules (inject into sub-agent prompts when relevant)

### TypeScript + Bun monorepo (always applies to code changes)

- Run typecheck/tests FROM `code/` directory, not workspace root
- Use `bat`, `eza`, `fd`, `rg`, `sd` — NOT `cat`, `ls`, `find`, `grep`, `sed`
- File imports inside `code/` use `.ts` extensions (`import x from "./x.ts"`)
- Types: strict mode on; prefer `type` imports via `verbatimModuleSyntax`
- Never build after changes (per user global rule); tests verify correctness

### Commits & PRs

- Conventional commits ONLY. Never add `Co-Authored-By` or AI attribution.
- Do NOT amend, force-push to main, or skip hooks unless user explicitly asks.
- User must authorize before creating PRs / pushing to shared state.

### Testing (Strict TDD Mode: ENABLED)

- Test runner: `bun test` (with `./` prefix if filename lacks `.test`/`.spec`)
- For exercise tests: use `resolveExerciseFile(import.meta.url)` + `AIDEV_TARGET` env var
- Assertions: target STRUCTURE (model used, tool names, shape) over literal LLM text
- Integration tests hit real API — guard on `ANTHROPIC_API_KEY` in `beforeAll`

### Exercise contract (when editing `packages/exercises/**`)

- Required files: `exercise.md`, `starter.ts`, `solution.ts`, `tests.test.ts`, `meta.json`
- `exercise.md` must have 6 sections (see `docs/EXERCISE-CONTRACT.md`)
- `starter.ts` MUST have a `// Docs:` comment header with canonical URLs
- Canonical doc URLs: `platform.claude.com/...` (NOT `docs.anthropic.com` — it 301s)
- Default model in solutions: Claude Haiku (cost discipline)

### i18n (M3+)

- Content: `<exercise>/<locale>/exercise.md` (locales: `es`, `en`)
- CLI strings: `packages/cli/src/i18n/{es,en}.json`
- Default locale: `es` (LATAM first). Resolution: CLI flag → env `AIDEV_LOCALE` → config → default.
